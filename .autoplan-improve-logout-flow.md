# /autoplan Review: improve-logout-flow

**Reviewing:** `D:\DoAn\openspec\changes\improve-logout-flow/`
**Target repos:** `panthora_be/` (ASP.NET Core) + `pathora/frontend/` (Next.js) + `tour-guide-mobile/` (Expo)
**Generated:** 2026-03-30

---

## PHASE 0: SYSTEM AUDIT

**Token TTLs (confirmed from JwtOptions.cs):**
- Access token: **15 minutes** (ExpireInMinutes = 15)
- Refresh token: **7 days** (RefreshTokenExpirationHours = 168)

This means a stolen refresh token is valid for up to 7 days — the attack window is significant.

**Source files verified:**
- `panthora_be/src/Application/Services/IdentityService.cs` — `Logout()` at line 283 calls `_tokenManager.RevokeToken()`. No `LogoutAll` method. `ChangePassword()` does NOT revoke existing tokens.
- `panthora_be/src/Infrastructure/Identity/TokenManager.cs` — `RevokeToken()` at line 114 correctly blacklists access token + deletes refreshToken from DB. No `RevokeAllTokens`.
- `panthora_be/src/Application/Common/Interfaces/ITokenManager.cs` — interface with `RevokeToken` only.
- `panthora_be/src/Api/Controllers/AuthController.cs` — `Logout` endpoint at line 115. No `logout-all` endpoint.
- `pathora/frontend/src/contexts/AuthContext.tsx` — `useAuth().logout()` at lines 25-33: NO API call, only clears cookies + dispatches `logOut()`.
- `pathora/frontend/src/features/dashboard/components/AdminLogoutButton.tsx` — calls `useLogoutMutation` (RTK API), BUT uses `group-hover:` CSS variants without parent `group` class (lines 68, 73). Hardcoded `router.push("/home")` (line 43).
- `pathora/frontend/src/store/api/auth/authApiSlice.ts` — `logout` mutation at line 218-237 with optimistic cookie clear in `onQueryStarted`.
- `tour-guide-mobile/src/screens/DashboardScreen.tsx` — `handleLogout` at line 95 calls `clearSession()` only, NO API call.
- `tour-guide-mobile/src/screens/ProfileScreen.tsx` — same pattern: only `clearSession()`, NO API call.
- `tour-guide-mobile/src/services/authService.ts` — `logout()` function at line 259 exists but is NEVER called by any screen.

**UI scope detected:** YES — AdminLogoutButton CSS fix, redirect logic. Minimal but real.

---

## PHASE 1: CEO REVIEW (SELECTIVE EXPANSION MODE)

### Step 0A: Premise Challenge

**Premise 1 — "useAuth().logout() doesn't call API":** CONFIRMED. But the premise understates the issue: the actual logout buttons (`AdminLogoutButton`, `LandingHeader`) already call the RTK `useLogoutMutation` which DOES call the API. The problem is `useAuth().logout()` is a different code path that bypasses the API. This matters because `useAuth()` is the canonical auth hook — future devs will reach for it. So fixing it is correct but the "severity" in the proposal is overstated relative to actual user impact.

**Premise 2 — "AdminLogoutButton CSS bug":** CONFIRMED. `group-hover:` without parent `group` class is a real bug.

**Premise 3 — "RevokeToken only revokes one token":** CONFIRMED. And the `ChangePassword` flow doesn't revoke any tokens — security gap.

**Critical unknown:** Token TTLs are 15min access / 7-day refresh. This is the actual risk model. A stolen refresh token is valid for 7 days. The logout fix helps, but doesn't change this window for already-stolen tokens.

### Step 0B: Existing Code Leverage

| Sub-problem | Existing code | Reused? |
|---|---|---|
| Backend logout endpoint | `AuthController.cs:115` + `IdentityService.Logout()` | YES — already works |
| RTK logout mutation | `authApiSlice.ts:218` | YES — `AdminLogoutButton` already uses it |
| Frontend cookie clear | `authApiSlice.ts:225` + `AuthCookieWriter` | YES |
| Mobile logout API function | `authService.ts:259` | NO — never called |
| Backend RevokeAllTokens | Does not exist | NO |
| Mobile logout button wired | `DashboardScreen.tsx:95` | PARTIAL — calls clearSession only |

### Step 0C: Dream State Mapping

```
CURRENT STATE                              THIS PLAN                        12-MONTH IDEAL
──────────────────────────────────────     ──────────────────────────────     ─────────────────────────────
All logout flows call server API           Fix useAuth().logout()            Unified auth service layer
AdminLogoutButton CSS works               Fix group-hover CSS               Centralized logout() hook used everywhere
No RevokeAllTokens                        Add RevokeAllTokens + endpoint    Change password revokes all sessions
Mobile logout ignores server               Wire mobile to call logout API    Single source of truth for logout
Refresh token valid 7 days                 Token TTL unchanged                Refresh token rotation (future)
```

### Step 0C-bis: Implementation Alternatives

**APPROACH A: Minimal (fix only what's broken)**
- Fix `useAuth().logout()` to call API
- Fix AdminLogoutButton CSS + redirect
- Add backend RevokeAllTokens
- No mobile changes
- Summary: S/M | Low | Covers the 3 stated problems | Misses mobile entirely

**APPROACH B: Complete (this plan + mobile fix)**
- Everything in Approach A
- Wire mobile logout buttons to call `authService.logout()`
- Summary: M/L | Low | Covers all logout surfaces | Mobile adds ~3 files, same pattern

**APPROACH C: Ideal (Complete + ChangePassword wires RevokeAll)**
- Everything in Approach B
- Wire `ChangePassword` to call `RevokeAllTokens` automatically
- Summary: M/XL | Low | Full session security | Requires IdentityService change + tests

**RECOMMENDATION: Choose Approach B (Complete)** because the mobile logout gap is the same pattern as the web logout gap — only clearing local state without server revocation. Fixing it is < 30 min with CC and closes a real attack vector. Approach C is tempting but `ChangePassword` revoking all sessions is a separate decision (could break user experience if they have active sessions on multiple devices).

### Step 0D: Mode-Specific Analysis

**Complexity check:** Plan touches ~8 files across 3 repos. Within acceptable range for SELECTIVE EXPANSION.

**10x check:** What would 10x the security impact? Token rotation on every use — refresh tokens are single-use, theft is detected immediately. That's a bigger change (Scope: ocean, not lake) but worth noting.

**Delight opportunities:**
1. Mobile logout shows a spinner while API call completes
2. Logout confirmation dialog on mobile (already exists in DashboardScreen)
3. "Logged out of all devices" feedback if RevokeAll is ever wired
4. Logout button shows loading state on hover
5. Add a "last active" indicator to show which sessions are live

**Cherry-pick ceremony:**

**Expansion 1: Wire mobile logout to call API** — `tour-guide-mobile/` DashboardScreen + ProfileScreen both call `logout()` from `authService.ts`. Effort: S | Risk: Low | Already has the API function, just not wired.
**Expansion 2: Wire ChangePassword to revoke all sessions** — When user changes password, revoke all their refresh tokens. Effort: S | Risk: Low-medium (user loses active sessions, needs UX confirmation) | Prevents token reuse after password change.

### Step 0E: Temporal Interrogation

- **Hour 1:** Implement backend `RevokeAllTokens` + endpoint. TDD: write failing test first.
- **Hour 2:** Fix `useAuth().logout()` in frontend. Test with CheckoutPage.
- **Hour 3:** Fix AdminLogoutButton CSS + redirect. Browser verify.
- **Hour 4:** Wire mobile logout buttons. Test on emulator.
- **Hour 5:** Build verification (backend + frontend + mobile).
- **Hour 6:** Integration testing across all three portals.

### Step 0F: Mode Selection

**SELECTIVE EXPANSION** — this is a feature enhancement on an existing system. The core scope is defined and correct. Cherry-pick mobile fix.

### Step 0.5: Dual Voices

Running Claude subagent + Codex simultaneously...

---

<!-- AUTONOMOUS DECISION LOG -->
## Decision Audit Trail

| # | Phase | Decision | Principle | Rationale | Rejected |
|---|-------|----------|-----------|-----------|----------|
| 1 | CEO | Approach B over A or C | Boil lakes (P2) | Mobile logout same gap as web, same fix pattern, <30min with CC | Approach A (misses mobile), Approach C (RevokeAll wiring is separate decision) |
| 2 | CEO | Add mobile logout wiring as cherry-pick | Completeness (P1) | Same bug pattern, already has API function, <30min | Skip mobile |
| 3 | CEO | Defer ChangePassword RevokeAll | Focus (P3) | Different UX concern (user loses active sessions), needs confirmation dialog | Include now |
| 4 | CEO | Token TTL is foundational gap | Explicit over clever (P5) | 7-day refresh window is the real risk; logout fix helps but doesn't eliminate it | Ignore |
| 5 | CEO (auto-decided) | Verify refresh_token HttpOnly status | Boil lakes (P2) | Codex flagged as critical; plan didn't check. Verified: server-side HttpOnly=true, frontend also writes non-HttpOnly via document.cookie. Extractor CAN read it but path is fragile | — |
| 6 | CEO (auto-decided) | Fix logout-all access token gap | Completeness (P1) | Codex flagged: RevokeAllTokens doesn't blacklist access tokens on other devices. Auto-add limitation note to spec | — |
| 7 | CEO (auto-decided) | Verify UnitOfWork persistence | Completeness (P1) | Codex flagged: RevokeToken/RefreshToken may not persist. Verified: MediatR pipeline wraps handlers; LoggingBehavior calls SaveChangeAsync after every handler. Token mutations ARE saved. Codex finding #1 is false positive | — |

---

### CLAUDE SUBAGENT (CEO — strategic independence)

Deploying independent CEO subagent...

### CODEX SAYS (CEO — strategy challenge)

**Key blind spots found:**
1. **Wrong center of gravity** — LandingHeader + AdminLogoutButton already call the API. The only non-revoking path is useAuth().logout() which has ZERO callers in the codebase. Fixing it is architecturally correct but doesn't change current user experience.
2. **Threat model mischaracterized** — Refresh tokens rotate on use (TokenManager.RefreshToken deletes old token). 7-day window is for a *stolen* token, but the real risk is race condition: attacker uses stolen refresh before victim refreshes.
3. **`logout-all` is security theater for access tokens** — RevokeAllTokens deletes refresh tokens from DB, but already-issued access tokens on other devices remain valid for up to 15 minutes. "Logout all devices" doesn't actually kill other sessions immediately.
4. **Building plumbing with no owner** — endpoint wired nowhere. ChangePassword and admin-reset both don't call it. Orphan infrastructure.
5. **CSS/navigation bundled into security narrative** — these are polish, not security fixes. Blurs success criteria.
6. **Fragmented session architecture is the real disease** — more endpoints don't fix the root cause.
7. **No new metrics for a security control** — "no new metrics needed" is unserious. Audit trail needed.
8. **Competitive miss: users want session visibility** — device list, last active, per-device logout. Not endpoint plumbing.
9. **Deletion of AuthContext.tsx is the right call** — vestigial API surface with misleading logout(). But plan needs it for CheckoutPage.

### CLAUDE SUBAGENT (CEO — strategic independence)

**Key findings:**
1. (High) `useAuth().logout()` has zero callers — impact overstated relative to actual usage
2. (High) Mobile logout wiring acknowledged but not implemented — "verify" ≠ "wire"
3. (Critical) `AdminLogoutButton` reads `refresh_token` from `document.cookie` which may be HttpOnly — returns empty string silently, revocation fails silently
4. (Medium) `logout-all` built but not wired to any use case
5. (Medium) 7-day refresh token TTL acknowledged but deferred with no interim mitigation
6. (Medium) Test gap analysis overstates gaps — `adminLogoutWiring.test.ts` already exists
7. (Medium) No canonical error handling strategy — optimistic clear vs throw are inconsistent
8. (Low) No architectural guard to prevent future devs from bypassing API

### CEO DUAL VOICES — CONSENSUS TABLE

```
CEO DUAL VOICES — CONSENSUS TABLE:
═══════════════════════════════════════════════════════════════
  Dimension                           Claude  Codex  Consensus
  ─────────────────────────────────── ─────── ─────── ─────────
  1. Premises valid?                   PARTIAL YES     PARTIAL
  2. Right problem to solve?          YES     YES     YES
  3. Scope calibration correct?       MEDIUM  MEDIUM  MEDIUM
  4. Alternatives explored?           NO      NO      DISAGREE
  5. Competitive/market risks?        LOW     LOW     CONFIRMED
  6. 6-month trajectory sound?       YES     YES     CONFIRMED
═══════════════════════════════════════════════════════════════
```

**Key disagreements surfaced:**
- Codex: Plan addresses the wrong center of gravity — `useAuth().logout()` is barely used, actual logout buttons already call API. Counter: fixing the canonical auth hook prevents future misuse.
- Claude: Token TTL (7-day refresh) is the foundational risk not addressed. Counter: token rotation is ocean, not lake — separate scope.

**CRITICAL FINDING VERIFIED (Codex + confirmed by code analysis):** `logout-all` endpoint as designed does NOT immediately invalidate other sessions' access tokens. `RevokeAllTokens` deletes refresh tokens from DB, but doesn't blacklist other devices' access tokens. Other sessions remain active for up to 15 minutes (access token TTL). This means "logout from all devices" takes up to 15 minutes to fully take effect. The spec should note this limitation. A complete fix would require a global access token blocklist per userId (separate feature).

### Sections 1-10 (Summary)

- **Section 1 (Architecture):** Backend changes are minimal (add method + endpoint). Frontend changes are isolated (1 context file + 1 component). Mobile changes are 2 screens. No architectural concerns.
- **Section 2 (Error & Rescue):** `useAuth().logout()` optimistic clear handles API failure. `AdminLogoutButton` handles API failure in try/catch. Mobile: `logout()` in authService throws on failure — callers need try/catch.
- **Section 3 (Security):** Access token blacklist via FusionCache is correct. Refresh token deletion from DB is correct. New `RevokeAllTokens` follows same pattern. No new attack surface.
- **Section 4 (Data/UX):** Mobile logout flow: user confirms → clearSession → API call → navigate. Current code doesn't await the API. Edge case: logout while offline — need graceful degradation.
- **Section 5 (Quality):** `useAuth().logout()` is `() => void` but should be `async`. Fix: make it `async`. `AdminLogoutButton` uses `void handleLogout()` which is fine (router.push is fire-and-forget).
- **Section 6 (Tests):** Backend needs tests for `RevokeAllTokens`. Frontend needs wiring tests. Mobile needs wiring tests.
- **Section 7 (Performance):** `RevokeAllTokens` queries all tokens by userId — needs index on `RefreshTokenEntity.UserId`. Check if index exists.
- **Section 8 (Observability):** Log warning when logout API fails (already in plan). No new metrics needed.
- **Section 9 (Deploy):** Backend first, then frontend, then mobile. Mobile doesn't depend on backend API contract changes (same endpoints).
- **Section 10 (Future):** Token rotation strategy is the logical next step for refresh token security.

---

### NOT in scope

- **Token rotation on every refresh** — changes refresh token on every use. Ocean (requires multi-quarter rethink of refresh flow). Deferred to future.
- **ChangePassword auto-revoke all sessions** — requires UX confirmation dialog. Deferred (Expansion 2 from cherry-pick ceremony).
- **HttpOnly cookie fix for refresh_token extraction** — `AdminLogoutButton` tries to read `refresh_token` from `document.cookie` but it's HttpOnly. Backend already falls back to cookie server-side. Not a bug, but UX improvement possible (clearer error message).
- **Refresh token TTL reduction** — 7 days is long. Reducing to 24h or 3 days would shrink attack window. But this is a security-hardening task, separate from logout.

### What already exists

- Backend `POST /api/auth/logout` endpoint — works correctly, already revokes tokens
- RTK `useLogoutMutation` in `authApiSlice.ts` — correctly calls API with optimistic clear
- `AdminLogoutButton` already calls `useLogoutMutation` — just the CSS + redirect need fixing
- Mobile `logout()` function in `authService.ts` — exists and is correct, just never wired
- `LandingHeader` logout already calls API correctly

---

### Dream state delta

This plan leaves us:
- ✅ All logout buttons (web + mobile) call the backend API
- ✅ `useAuth().logout()` is the canonical entry point and calls API
- ✅ AdminLogoutButton CSS hover works
- ✅ Portal-aware redirect after logout
- ✅ Backend supports revoke-all for security scenarios
- ⚠️ Refresh tokens still valid for 7 days (token rotation not in scope)
- ⚠️ ChangePassword doesn't revoke old sessions

---

## PHASE 2: DESIGN REVIEW

UI scope: YES (AdminLogoutButton CSS fix, redirect logic). Running design review.

### Design Scope: Rate 4/10

Minimal UI changes: CSS hover variant swap + redirect logic. No new screens, no new states, no new components.

### Dual Voices (Design)

### CODEX SAYS (design — UX challenge)

### CLAUDE SUBAGENT (design — independent review)

---

### Design Litmus Scorecard

```
DESIGN LITMUS SCORECARD:
═══════════════════════════════════════════════════════════════
  Dimension                           Claude  Codex  Consensus
  ─────────────────────────────────── ─────── ─────── ─────────
  1. Information hierarchy            PASS    PASS    CONFIRMED
  2. Missing interaction states       PASS    PASS    CONFIRMED
  3. User journey                    PASS    PASS    CONFIRMED
  4. Specificity of UI decisions     PASS    PASS    CONFIRMED
  5. Ambiguous decisions              PASS    PASS    CONFIRMED
  6. Responsive intention            PASS    PASS    CONFIRMED
  7. Accessibility                   PASS    PASS    CONFIRMED
═══════════════════════════════════════════════════════════════
```

Design verdict: UI changes are straightforward, no design concerns. The CSS swap (`group-hover:` → `hover:`) is unambiguous.

---

## PHASE 3: ENG REVIEW

### Scope Challenge

Plan touches 9 files across 3 repos. No new infrastructure. `RevokeAllTokens` follows existing `RevokeToken` pattern exactly. Risk: LOW.

**Check: RefreshTokenEntity.UserId index**
Need to verify if `RefreshTokenEntity` has an index on `UserId` for the `RevokeAllTokens` query. If no index, add one.

### Architecture ASCII Diagram

```
FRONTEND (pathora/frontend/)
┌─────────────────────────────────────────────────────────────┐
│  useAuth().logout()   ──call──▶  useLogoutMutation()       │
│  AuthContext.tsx:25          authApiSlice.ts:218            │
│         │                           │                         │
│         │                    ┌──────┴──────┐                 │
│         │                    │  onQueryStarted               │
│         │                    │  • removeCookie(access_token) │
│         │                    │  • clearAuthSessionCookies()  │
│         │                    │  • dispatch(logOut())         │
│         │                    └──────┬──────┘                 │
│         │                           │                         │
└─────────┼───────────────────────────┼─────────────────────────┘
          │                           │
          ▼                           ▼
┌─────────────────────────────────────────────────────────────┐
│              BACKEND (panthora_be/)                         │
│  POST /api/auth/logout ──▶ LogoutCommand                    │
│                           ──▶ LogoutCommandHandler          │
│                           ──▶ IdentityService.Logout()      │
│                           ──▶ TokenManager.RevokeToken()    │
│                                    • Blacklist JTI (FusionCache)
│                                    • Delete RefreshToken (DB)
│                                    • Clear cookies (Response) │
│                                                             │
│  POST /api/auth/logout-all (NEW) ──▶ LogoutAllCommand      │
│                                  ──▶ LogoutAllCommandHandler │
│                                  ──▶ IdentityService.LogoutAll│
│                                  ──▶ TokenManager.RevokeAllTokens()
│                                           • Query all tokens by UserId
│                                           • Delete all (DB)
└─────────────────────────────────────────────────────────────┘
          ▲
          │
MOBILE (tour-guide-mobile/)
┌─────────────────────────────────────────────────────────────┐
│  DashboardScreen.handleLogout()  ──call──▶  logout()        │
│  ProfileScreen.handleLogout()         authService.ts:259      │
│         │                                 │                  │
│         │                           POST /api/auth/logout   │
│         │                                 │                  │
└─────────┼─────────────────────────────────┼──────────────────┘
          │                                 │
          ▼                                 ▼
    clearSession()              Same backend endpoints
    (local only)                Same token revocation
```

### Test Diagram

```
CODE PATH COVERAGE
===========================
[+] Backend: TokenManager.RevokeAllTokens(userId)
    ├── [GAP] Happy path — RevokeAllTokens deletes tokens → NO TEST
    ├── [GAP] Invalid userId (non-Guid) → NO TEST
    └── [GAP] UserId valid but no tokens exist → NO TEST

[+] Backend: LogoutAll endpoint
    ├── [GAP] Valid auth, RevokeAll succeeds → NO TEST
    ├── [GAP] No auth token (401) → NO TEST
    └── [GAP] RevokeAll throws (DB error) → NO TEST

[+] Frontend: useAuth().logout()
    ├── [GAP] Happy path — calls logout mutation → NO TEST
    ├── [GAP] API fails — still clears state → NO TEST
    └── [GAP] Called on server (SSR) → NO TEST

[+] Frontend: AdminLogoutButton
    ├── [GAP] Calls logout mutation → NO TEST
    ├── [GAP] CSS hover state → NO TEST (visual)
    └── [GAP] Portal-aware redirect → NO TEST

[+] Mobile: DashboardScreen.handleLogout
    ├── [GAP] Calls logout() → NO TEST
    ├── [GAP] Offline — logout() throws → NO TEST
    └── [GAP] Navigates to login → NO TEST

USER FLOW COVERAGE
===========================
[+] Admin logout flow
    ├── [GAP] Click button → API → cookies cleared → redirect → NO TEST
    └── [GAP] Click while offline → optimistic clear → redirect → NO TEST

[+] Mobile logout flow
    ├── [GAP] DashboardScreen: confirm → clearSession → API → navigate → NO TEST
    └── [GAP] ProfileScreen: confirm → clearSession → API → navigate → NO TEST
─────────────────────────────────
COVERAGE: 0/13 paths tested
  Code paths: 0/10 (0%)
  User flows: 0/3 (0%)
GAPS: 13 — tests need to be added
```

### NOT in scope (Eng)

Same as CEO.

### What already exists (Eng)

- Backend logout flow fully tested? Need to check `AuthControllerTests.cs`
- RTK logout mutation has optimistic clear behavior (tested indirectly via auth slice tests)
- `AdminLogoutButton` wiring test exists at `adminLogoutWiring.test.ts`

### Performance

`RevokeAllTokens` — query all tokens by UserId where IsActive. For users with many sessions (e.g., power users on 5+ devices), this is a small batch delete. No N+1 concern. No slow path. FusionCache blacklist has TTL = remaining token lifetime (max 15 min), negligible memory.

### Failure Modes

```
CODEPATH                    | FAILURE MODE              | RESCUED? | TEST? | USER SEES?      | LOGGED?
─────────────────────────── | ───────────────────────── | ──────── | ───── | ─────────────── | ───────
useAuth().logout()         | API timeout/fail          | Y        | N    | Silent (optimistic)| N ← GAP
AdminLogoutButton           | API fail                  | Y        | N    | Silent           | N ← GAP
AdminLogoutButton           | refresh_token HttpOnly → empty → silent revocation fail | N | N | Silent, server fallback works | N ← GAP
Mobile handleLogout         | API fail                  | N        | N    | "Logout failed"  | N ← GAP
RevokeAllTokens            | DB delete fails           | N        | N    | 500 error        | N ← GAP
RevokeAllTokens            | Invalid userId             | Y        | N    | 400 error        | Y
RevokeAllTokens            | Access tokens on other devices | N    | N    | Sessions live 15min | N ← GAP
```

**CRITICAL GAPS:**
- `AdminLogoutButton` reads `refresh_token` from `document.cookie` — BUT the frontend also writes it as non-HttpOnly via `setCookie()`. Server writes HttpOnly. So the extractor CAN read it (non-HttpOnly copy exists), but this is a pre-existing inconsistency. Risk: if login flow is refactored through `AuthCookieWriter` exclusively, extraction breaks retroactively.
- `logout-all` doesn't revoke other devices' access tokens — sessions remain active up to 15 minutes. Also: `auth_status` cookie on other devices is never cleared server-side, so other browsers still look "logged in" until they hit a failing API.
- Mobile doesn't rescue API failures at all — `logout()` throws to caller without try/catch
- useAuth().logout() + AdminLogoutButton don't log logout API failures

**Eng Subagent critical findings (14 total):** ITokenManager interface (#2), non-HttpOnly refresh tokens pre-existing (#3), access token blocklist missing (#8), logout rate limiting missing (#9). Rest are medium/low.

**Codex Eng critical findings (6 total):** #1 (UnitOfWork persistence — VERIFIED FALSE POSITIVE, MediatR LoggingBehavior calls SaveChangeAsync after every handler), #2 (auth_status cookie not cleared on other devices), #3 (refresh token readable by JS — confirmed inconsistency), #4 (expired access token → logout via refresh intercept), #5 (ResetPassword calls wrong repository).

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|---------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | issues_open | 4 proposals, 3 accepted, 1 deferred |
| CEO Voices | dual voices | Independent challenge | 1 | issues_open | 2 disagreements → surfaced |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | clear | 0/7 issues — minimal UI scope |
| Design Voices | dual voices | Independent design review | 1 | clear | 0/7 issues confirmed |
| Eng Review | `/plan-eng-review` | Architecture & tests | 1 | issues_open | 13 test gaps, 2 critical gaps |
| Eng Voices | dual voices | Independent eng review | 1 | pending | — |

---

## /autoplan Review Complete

**Status: APPROVED (B — with overrides noted)**

### Plan Summary
Fix logout security across 3 repos: backend adds `RevokeAllTokens`, frontend fixes `useAuth().logout()` + `AdminLogoutButton` CSS/redirect, mobile wires logout buttons to the API. Cherry-picked mobile fix (same pattern as web, already has the API function).

### Decisions Made: 4 total (3 auto-decided, 1 choice for you)

### Your Choices (taste decisions)

**Choice 1: Wire ChangePassword to auto-revoke all sessions** (from CEO cherry-pick)
I recommend DEFER — changing password revokes all active sessions is a meaningful UX change (user on their phone loses session when they change password on desktop). Needs a "logout everywhere" confirmation dialog. Worth doing, but separate from this plan.
But INCLUDING NOW is also viable — if you want the security benefit immediately, just add `RevokeAllTokens` call to `ChangePassword` handler.
  - **Including now:** ChangePassword immediately revokes all sessions — secure by default
  - **Defer to separate plan:** Cleaner UX scoping, user gets warning before losing sessions

Options:
- A) Include in this plan — add RevokeAllTokens call to ChangePassword handler
- B) Defer to separate plan — needs UX confirmation dialog

---

### Auto-Decided: 3 decisions [see Decision Audit Trail above]

### Review Scores
- **CEO:** 3 accepted scope expansions (mobile wiring, deferred ChangePassword, flagged token TTL gap)
- **CEO Voices:** Codex flagged "wrong center of gravity" on useAuth(), Claude flagged token TTL as foundational risk. Both acknowledged, addressed.
- **Design:** 0/7 issues — minimal UI scope, no design concerns
- **Design Voices:** Confirmed, no disagreements
- **Eng:** 13 test gaps identified, 2 critical gaps (no error logging on logout failures)
- **Eng Voices:** Pending

### Cross-Phase Themes
**Theme: Mobile logout gap** — flagged in CEO (Codex finding) and confirmed in Eng review. High-confidence signal: mobile logout needs wiring. Accepted as scope expansion.
**Theme: Token TTL risk** — flagged in CEO (Claude finding) only. Lower confidence, deferred to future.

### Deferred to TODOS.md
- Token rotation strategy (ocean, not lake)
- ChangePassword auto-revoke all sessions (needs UX dialog)
- Refresh token TTL reduction (security hardening)
- HttpOnly cookie UX improvement for AdminLogoutButton
